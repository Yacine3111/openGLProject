# OpenGL Practice Project

This project is a personal practice to learn and experiment with **OpenGL**.

If you are using **CLion**, make sure to place the `glfw3.dll` file inside the `cmake-build-debug` directory so the application can run correctly.
